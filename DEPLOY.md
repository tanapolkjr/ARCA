# วิธี Deploy ARCA E-Service (ตั้งแต่ศูนย์)

คู่มือนี้เขียนให้ทำตามได้แม้ไม่ใช่โปรแกรมเมอร์ — ทำตามลำดับข้อ 1 → 7 **ห้ามข้าม ห้ามสลับลำดับ**

สิ่งที่ต้องมีก่อนเริ่ม: บัญชี GitHub, บัญชี Supabase, บัญชี Vercel (สมัคร Vercel ด้วย GitHub จะง่ายที่สุด) และไฟล์ zip ของโปรเจกต์

**ภาพรวม 3 ส่วนที่ต้องเชื่อมกัน**

| ส่วน | ทำหน้าที่ | ตั้งที่ไหน |
|---|---|---|
| Supabase | ฐานข้อมูล + ระบบ Login + ที่เก็บไฟล์ | supabase.com |
| GitHub | เก็บโค้ด | github.com |
| Vercel | เอาโค้ดจาก GitHub ไปทำเป็นเว็บจริง | vercel.com |

ลำดับที่ถูกคือ **Supabase ก่อน → GitHub → Vercel** เพราะ Vercel ต้องรู้ค่าเชื่อมต่อ Supabase ตอน build

---

## 1. สร้างโปรเจกต์ Supabase

1. ไปที่ [supabase.com](https://supabase.com) → login → **New Project**
2. ตั้งชื่อโปรเจกต์ (เช่น `arca`)
3. ตั้ง **Database Password** — เก็บไว้ที่ปลอดภัย (ใช้ตอนกู้ระบบ ไม่ใช่รหัส login เว็บ)
4. **Region: Singapore** (ใกล้ไทยที่สุด = เว็บเร็วกว่า)
5. รอสร้างเสร็จ 1–2 นาที

## 2. รัน Database Schema — 23 ไฟล์ เรียงตามลำดับ

ในเมนูซ้ายของ Supabase กด **SQL Editor** → **New query** แล้วทำแบบนี้ทีละไฟล์:
เปิดไฟล์จากโฟลเดอร์ `supabase/migrations/` → คัดลอกทั้งไฟล์ → วางใน SQL Editor → กด **Run** → ต้องขึ้น `Success`

รันเรียงตามนี้ ห้ามข้ามแม้แต่ไฟล์เดียว:

| # | ไฟล์ | สร้างอะไร |
|---|---|---|
| 1 | `0001_init.sql` | ตารางหลักทั้งหมด + ระบบ Role + ทริกเกอร์สร้างโปรไฟล์ |
| 2 | `0002_storage.sql` | ที่เก็บไฟล์ `smart-living-files` (เพดาน 500MB) |
| 3 | `0003_chat.sql` | แชทภายในทีม |
| 4 | `0004_fixes.sql` | จำกัดสิทธิ์เคลื่อนไหวสต็อกให้ Manager/Store |
| 5 | `0005_fixes2.sql` | แก้บั๊กแชท + เพิ่มสิทธิ์ลบหลายโมดูล |
| 6 | `0006_chat_rls_reset.sql` | รีเซ็ตสิทธิ์แชท |
| 7 | `0007_delete_policies.sql` | สิทธิ์ลบของ Stock/Ticket/Contact |
| 8 | `0008_chat_rls_bulletproof.sql` | สิทธิ์แชทเวอร์ชันสุดท้าย (โปรเจกต์ใหม่ให้รันด้วยเสมอ) |
| 9 | `0009_pm_request_files.sql` | ไฟล์แนบ/ลิงก์ของ PM Request |
| 10 | `0010_stock_subcategory.sql` | ช่อง Sub-Category ของสินค้า |
| 11 | `0011_cancel_withdrawal.sql` | ยกเลิกการเบิกใน Install Period |
| 12 | `0012_borrow_redesign.sql` | ระบบยืมคืนหลายรายการ |
| 13 | `0013_launch_recheck_fixes.sql` | แก้บั๊กจาก 0012 + ย้ายคลังตัดสต็อกจริง |
| 14 | `0014_sourcing_module.sql` | โมดูล Sourcing 7 ตาราง + สิทธิ์ Super Admin/Manager + ที่เก็บไฟล์ `product-media` |
| 15 | `0015_sourcing_to_inventory.sql` | ช่องเชื่อมสินค้า Sourcing ที่ Approved → Inventory |
| 16 | `0016_accounting_foundation.sql` | โมดูลบัญชี: บริษัทผู้ออกเอกสาร · เลขที่เอกสาร · ผู้ขาย · เอกสารขาย/ซื้อ · สมุดรายรับ-รายจ่าย |
| 17 | `0017_accounting_tags_and_numbering.sql` | เลขเอกสารรายเดือน · Tag ประเภทงาน · ส่วนลดเป็น % |
| 18 | `0018_ar_payments.sql` | รับชำระเงิน · ตัดยอดบิล · คำนวณสถานะอัตโนมัติ |
| 19 | `0019_stock_sale_price.sql` | ราคาขายในคลังสินค้า (ดึงจาก Sourcing / คีย์เอง) |
| 20 | `0020_cashbook_delete_fix.sql` | ปลดล็อกการลบรายการในสมุดรายรับ-รายจ่าย |
| 21 | `0021_line_wht.sql` | หัก ณ ที่จ่ายรายบรรทัด (หักเฉพาะค่าบริการ) |
| 22 | `0022_po_no_and_extra_discount.sql` | เลขที่ PO ของลูกค้า · ส่วนลดพิเศษท้ายบิล |
| 23 | `0023_incoming_shipments.sql` | On the way: ล็อตขนส่งสินค้ากำลังมา + รับเข้าคลัง |

> **ห้ามรัน `RESET_full_wipe.sql`** — ไฟล์นั้นเป็นเครื่องมือลบข้อมูลทิ้งทั้งหมด ไม่ใช่ migration
>
> **ถ้าขึ้น error สีแดง ให้หยุดทันที** อย่ารันไฟล์ถัดไป ส่ง error มาให้ดูก่อน

ทำไมต้องรันเอง ไม่ auto: เพื่อให้เห็นและควบคุมการเปลี่ยนแปลงฐานข้อมูลทุกครั้ง — โค้ดฝั่งเว็บจะพังถ้าตารางยังไม่ถูกสร้าง จึงต้องรันให้ครบ **ก่อน** deploy เสมอ

## 3. สร้างบัญชีผู้ใช้ (ไม่มีหน้าสมัครสมาชิกเอง)

1. เมนูซ้าย → **Authentication** → **Users** → **Add user** → **Create new user**
2. กรอกอีเมล + รหัสผ่าน → **ติ๊ก Auto Confirm User** → Create
3. ทำซ้ำสำหรับพนักงานทุกคน

## 4. ⚠️ ตั้งตัวเองเป็น Super Admin — ข้ามข้อนี้ไม่ได้

ทุกบัญชีที่สร้างใหม่จะได้ role **`Sale`** โดยอัตโนมัติ ซึ่ง**เข้าเมนู Sourcing ไม่ได้ และแก้ Role ให้ใครไม่ได้** ถ้าไม่มีใครเป็น Super Admin เลย จะไม่มีทางแก้จากในเว็บได้ ต้องกลับมาแก้ที่ Supabase อยู่ดี

1. เมนูซ้าย → **Table Editor** → เลือกตาราง **`users`**
2. หาแถวอีเมลของตัวเอง → ดับเบิลคลิกช่อง **`role`**
3. เปลี่ยนเป็น **`Super Admin`** (พิมพ์ให้ตรงเป๊ะ มีเว้นวรรค ตัว S กับ A ใหญ่) → Save
4. คนอื่นตั้ง role ตามจริงได้ที่นี่เลย หรือรอให้คุณ login แล้วไปตั้งในหน้า **การตั้งค่า** ของเว็บก็ได้

role ที่ใช้ได้: `Super Admin` · `Manager` · `Sale` · `PM` · `Admin` · `Store`
**เมนู Sourcing เห็นเฉพาะ `Super Admin` กับ `Manager`** เท่านั้น

## 5. คัดลอกค่า API จาก Supabase

1. ใน Supabase: **Settings** (รูปเฟือง) → **API**
2. คัดลอก 2 ค่านี้เก็บไว้ (ใช้ในข้อ 7)
   - **Project URL** → `https://xxxxxxxx.supabase.co`
   - **anon public** key → ขึ้นต้นด้วย `eyJ...`

> ห้ามใช้ `service_role` key เด็ดขาด — ตัวนั้นมีสิทธิ์เต็มและห้ามอยู่ในโค้ดฝั่งเว็บ

## 6. อัปโหลดโค้ดขึ้น GitHub

1. [github.com](https://github.com) → **New repository** → ตั้งชื่อ (เช่น `ARCA`) → เลือก **Private** → Create
2. แตกไฟล์ zip ของโปรเจกต์ออกมาก่อน
3. ในหน้า repo กด **uploading an existing file** → ลากไฟล์และโฟลเดอร์ทั้งหมดจากในโฟลเดอร์ที่แตกไว้เข้าไป → Commit
   - ลากเฉพาะ**ของที่อยู่ข้างในโฟลเดอร์** ไม่ใช่ตัวโฟลเดอร์ครอบ — `package.json` ต้องอยู่ชั้นบนสุดของ repo
   - ห้ามอัปโหลด `node_modules` หรือ `.env.local`

## 7. Deploy บน Vercel

1. [vercel.com](https://vercel.com) → login ด้วย GitHub เดียวกัน
2. **Add New → Project** → เลือก repo ที่เพิ่งสร้าง
3. Framework Preset ต้องเป็น **Vite** (ปกติ Vercel เดาให้เอง)
4. **ก่อนกด Deploy** เปิด **Environment Variables** แล้วใส่ 2 ตัวจากข้อ 5:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

   ใส่ให้ครบทั้ง Production / Preview / Development
5. กด **Deploy** รอ 1–2 นาที → ได้ลิงก์เว็บ เช่น `arca.vercel.app`
6. เปิดเว็บ → login ด้วยบัญชีจากข้อ 3 → ควรเห็นเมนู **Sourcing** ในแถบซ้าย ถ้าตั้ง Super Admin ไว้ถูก

### หลังจากนี้ทุกครั้งที่แก้โค้ด

อัปโหลดไฟล์ที่เปลี่ยนขึ้น GitHub branch `main` → Vercel build ขึ้นเว็บใหม่ให้อัตโนมัติ

---

## แก้ปัญหาที่เจอบ่อย

**เว็บขึ้นหน้าขาว / login แล้วไม่ไปไหน**
เกือบทุกครั้งคือ Environment Variables ไม่ได้ใส่หรือใส่ผิด → เปิด DevTools (F12) แท็บ Console แล้วดูข้อความ ถ้าเห็น `Missing VITE_SUPABASE_URL` แปลว่ายังไม่ได้ใส่

**แก้ Environment Variable แล้วเว็บยังเหมือนเดิม**
ตอน Redeploy ต้อง **เอาเครื่องหมายถูกออกจาก "Use existing Build Cache"** ไม่งั้น Vercel เสิร์ฟไฟล์เก่าที่ build ด้วยค่าเดิม (สังเกตได้จากชื่อไฟล์ `index-*.js` ไม่เปลี่ยนหลัง deploy) — กับดักนี้เคยกินเวลาไปหนึ่งชั่วโมงมาแล้ว

**ไม่เห็นเมนู Sourcing**
role ยังไม่ใช่ `Super Admin` หรือ `Manager` → กลับไปข้อ 4 แล้ว refresh หน้าเว็บ

**กดอะไรก็ขึ้น error สิทธิ์ / บันทึกไม่ได้**
มักแปลว่า migration รันไม่ครบ → กลับไปข้อ 2 ไล่ดูว่ารันครบทั้ง 23 ไฟล์จริงไหม

**เว็บเข้าไม่ได้หลังไม่ได้ใช้งานนาน**
Supabase แพลนฟรีจะ pause โปรเจกต์อัตโนมัติหลังไม่มีทราฟฟิกประมาณ 1 สัปดาห์ → เข้า dashboard กด Resume ไม่ใช่บั๊กของโค้ด

## เรื่องอัปโหลดไฟล์ใหญ่

ระบบใช้ **Resumable Upload** (แบ่งชิ้นส่ง เน็ตหลุดแล้วส่งต่อได้) เพดานตั้งไว้ **500MB** ทั้งสองที่เก็บไฟล์:
`smart-living-files` (Project/Ticket) ตั้งใน `0002_storage.sql` และ `product-media` (รูปสินค้า/เอกสารโรงงานของ Sourcing) ตั้งใน `0014_sourcing_module.sql`
อยากเปลี่ยนเพดาน — แก้ตัวเลข `file_size_limit` ในไฟล์นั้นแล้วรันซ้ำใน SQL Editor ได้เลย (ทั้งสองไฟล์เขียนให้รันซ้ำได้)

> ชื่อที่เก็บไฟล์ยังเป็น `smart-living-files` หลังเปลี่ยนชื่อแอปเป็น ARCA — ตั้งใจไว้แบบนั้น เพราะชื่อนี้ถูกฝังอยู่ใน path ของไฟล์ทุกไฟล์ในฐานข้อมูล เปลี่ยนแล้วไฟล์เก่าจะหาไม่เจอ


---

## หลังรัน 0016 — ตั้งค่าโมดูลบัญชีก่อนออกเอกสารใบแรก

**เข้าเมนู บัญชี → ตั้งค่าบริษัท**

1. **แก้ข้อมูล "บริษัท อาร์ก้า โฮม จำกัด"** ที่ระบบสร้างไว้ให้ — ใส่เลขประจำตัวผู้เสียภาษี 13 หลัก, รหัสสาขา, ที่อยู่, เบอร์โทร
   ไม่มีเลขผู้เสียภาษี = ออกใบกำกับภาษีที่ใช้ได้จริงไม่ได้
2. **เพิ่มบัญชีธนาคาร** — พิมพ์ท้ายใบเสนอราคาและใบแจ้งหนี้
3. **ตั้งเลขเอกสารเริ่มต้น** (ปุ่ม "เลขเอกสาร") ให้ต่อจากระบบเดิมทุกประเภท
   ถ้าใบล่าสุดของเดือน ส.ค. 2026 คือลำดับที่ 12 → เลือก QT วันที่ใดก็ได้ในเดือน 08/2026 ใส่เลขล่าสุด 12 ระบบจะออกใบถัดไปเป็น `QT202608013`
   **ข้อนี้ข้ามไม่ได้** ถ้าออกเลขซ้ำกับที่ส่งลูกค้าไปแล้วคือปัญหาทางกฎหมาย
4. **เมนู รายรับ-รายจ่าย → จัดการกระเป๋าเงิน** — เพิ่มบัญชีธนาคารและเงินสดย่อยที่ใช้จริง พร้อมยอดยกมา

เมนูบัญชีและรายรับ-รายจ่ายเห็นเฉพาะ **Super Admin / Manager / Admin**


---

## ล้างข้อมูลทดสอบก่อนเริ่มใช้จริง

`supabase/RESET_accounting_testdata.sql` — ลบเอกสาร การรับชำระ และรายการในสมุดทั้งหมด
แล้วตั้งเลขเอกสารกลับไปเริ่มที่ `001`

**ไม่แตะ** บริษัท · บัญชีธนาคาร · ผู้ขาย · ประเภทงาน · กระเป๋าเงิน และไม่แตะโมดูลอื่นเลย

รันครั้งเดียวตอนวางระบบเสร็จ ก่อนออกเอกสารจริงใบแรก
